# 自我檢查

## 作業要求對照

- 5-10 頁投影片：已完成 7 頁 PPTX。
- 動機與目的：第 2 頁。
- 方法流程：第 4 頁。
- 預期結果：第 6 頁。
- 結論：第 7 頁。
- RAG、pseudo-query、HyDE、LLM、VLM、n8n：第 4-5 頁逐一說明。
- 加分程式碼：`src/scam_guard_demo.py` 與 `n8n/secondhand_scam_guard_workflow.json`。

## 測試結果

- `--test` 分級檢查：8/8 通過（低、中、高、拆字規避、同義詞、正常蝦皮、純問候、空白）。
- `pytest tests/`：37/37 通過（正規化、規則、新詐騙樣態、話術標註、融合、LLM fallback、連結/黑名單查核）。
- `tests/_human_sim.py`：模擬真人 20 情境 + 4 錯誤處理全通過（兩段式流程、零崩潰）。
- 已修正「不接受先匯款」被誤判為中風險的否定語境問題。
- 已修正中文 RAG token 重疊失效（改用 bigram 斷詞）。
- 已修正 HyDE 通用模板污染 RAG 檢索（改只用原文+pseudo-query 計分）。
- 已修正正常貨到付款 / 代購被誤判（generic 詞不入通用詞表，改由情境組合判斷）。
- 新增拆字規避（金．流．驗．證）與同義詞（轉帳／加賴／驗證碼）偵測。

## 新增功能

- 知識庫擴充至 10 類，含台灣最常見的「解除分期付款」、貨到付款換包裹、假投資、代購代儲。
- 原文話術標註（含拆字規避位置）、截圖 tesseract.js OCR、規則 vs LLM 對照面板、列印/匯出 PDF。
- **連結與電話查核（黑名單比對）**：先比對官方黑名單（5.8 萬筆 165／刑事局詐騙網域，`tools/sync_blocklist.py` 同步自 data.gov.tw），命中標「已被通報」；沒命中再用啟發式 + 話術/LLM，並附一鍵查證連結。
- **漸進式渲染**：規則結果毫秒級先顯示，4B LLM 完成再更新（前端兩段式 `?llm=0`）。

## 接 4B LLM（已實際接上並驗證）

- 已安裝免安裝版 Ollama 於 Downloads，模型 gemma3:4b（Q4_K_M），以 Vulkan GPU 推論。
- 規則引擎永遠離線執行；偵測到本機 Ollama 時自動加入 4B LLM 語意研判，並背景預熱模型。
- 實測：預熱後每次分析約 4–5 秒；規則=低、LLM=高（如「匯到私人帳號繞過平台」）→ 綜合取高，展現混合式價值。
- 融合策略：取規則與 LLM 的較高風險（保守），LLM 失敗自動重試一次，再失敗即降級純規則模式。
- LLM 用戶端僅用 Python 標準庫，無需額外套件；模型與端點以環境變數設定。

## 限制說明

- 這個系統只能做風險提醒與查證建議，不能保證 100% 判斷詐騙。
- 截圖可用本機 OCR（tesseract.js）自動轉文字；要「看懂圖」的真 VLM 需接 n8n 的 Gemini 節點。
- 黑名單比對涵蓋官方已通報網域；新詐騙網域未收錄時，靠話術引擎 + LLM 判斷（黑名單沒命中 ≠ 安全）。
- n8n 版為「判斷層」（規則 + RAG + 知識庫），連結/電話黑名單查核在本機 app 與排程腳本；若要 n8n 也具備，可把 link_check 邏輯移植進 Code node。
- 若要使用正式向量資料庫，可把 workflow 內建 Code node 的簡化 RAG 替換成 n8n Simple Vector Store、Qdrant 或其他 vector store。
