# 操作說明（使用手冊）

二手交易 AI 防踩雷助理 —— 貼上 FB／蝦皮／Threads 的交易對話或截圖文字，判斷是不是詐騙。
本文照「**在哪個地方、做什麼事**」一步步說明。

---

## 一、啟動程式

> 需求：Windows、已安裝 Python 3.10+（本專案只用標準庫，**不必 `pip install`**）。

| 想要 | 在哪裡做 | 怎麼做 |
|------|----------|--------|
| **一鍵啟動（建議）** | 專案資料夾 | 在 PowerShell 執行 `\.\Start-UI.ps1`：會自動開 Ollama → 進 4B LLM 模式 → 開瀏覽器 |
| 只開純規則模式 | 專案資料夾 | `python server.py`（不需 Ollama，少了小模型但功能完整） |
| 停止 | 專案資料夾 | `\.\Stop-UI.ps1` |

啟動成功會看到：`已啟動：http://127.0.0.1:8765`、`目前模式：4B LLM 模式（gemma3:4b）`。
瀏覽器打開 **http://127.0.0.1:8765** 即可使用。

---

## 二、第一次進來：選「研判模式」（跳出的視窗）

開啟網頁時會先跳一個視窗讓你選模式：

### 🟢 離線版（全部在本機跑、免金鑰）
選「離線版」後，**視窗下方會即時偵測本機小模型（gemma3:4b）狀態**：

- **✓ 本機 LLM 已就緒** → 直接按「**開始使用**」。分析時會加上小模型研判。
- **尚未下載模型** → 按「**下載 LLM（約 3.3GB）**」，會顯示**進度條**；下載完才能按「開始使用」。
  （已經有模型就不會出現下載鈕，也不會重抓。）
- **Ollama 未啟動** → 提示先跑 `\.\Start-UI.ps1`，再按「**重新檢查**」。
- 不想用小模型 → 按「**改用純規則模式**」，照樣能完整分析（規則＋知識庫＋RAG）。

### 🔵 線上版（用雲端 Gemini，要自備金鑰）
選「線上版」→ 視窗會出現 **API key 欄**，貼上你的 Gemini key（[Google AI Studio](https://aistudio.google.com/app/apikey) 取得）。
金鑰**只用於這次、不會儲存**。線上版的看截圖改用 Gemini VLM（更能「看懂」圖）。

> 隨時可按右上角「**切換模式**」重選。

---

## 三、主畫面：左邊輸入、右邊看結果

### 📍 左邊「交易內容」面板 —— 你要操作的地方

1. **截圖區（可放多張）**：把交易對話截圖**拖曳／貼上／按「選擇圖片」**放進來，會排成縮圖（每張右上角 ✕ 可單張刪除）。
2. **辨識按鈕**（依模式自動切換）：
   - 離線版：**「辨識文字 OCR」**（瀏覽器本機辨識，不上傳）
   - 線上版：**「用 Gemini 看圖（VLM）」**（多張會一起判讀）
3. **「解析 QR 收款碼」**：截圖裡有收款／釣魚 QR Code，按一下解出網址並自動查黑名單。
4. **文字框**：直接貼交易對話、私訊，或上面辨識出來的文字。**分析以這個文字框為準。**
5. **低／中／高風險**：三個範例按鈕，沒資料時可拿來試跑。
6. **「分析風險」**（紅色大鈕）：按下去就開始研判。
7. **「貼連結／電話快速查」**：不想打整段，直接貼可疑網址或電話 → 比對官方黑名單＋風險判斷。

### 📍 右邊「結果」面板 —— 看研判結果的地方

由上到下會出現：

| 區塊 | 看什麼 |
|------|--------|
| **風險等級＋分數** | 低／中／高 與 0–100 分，一眼看危險程度 |
| **規則 vs LLM 對照** | 左邊規則引擎、右邊小模型（離線顯示「本機 4B LLM」、線上顯示「雲端 Gemini」）兩種研判並排 |
| **詐騙攻擊鏈** | 對方走到詐騙劇本第幾步（建立接觸→製造急迫→誘導離開平台→金流收割），並**預測下一步** |
| **命中話術（原文標註）** | 把可疑句子在原文標色，連「金．流．驗．證」這種拆字也標得到 |
| **可疑訊號** | 命中的關鍵話術（金流驗證、訂金、加 LINE…） |
| **RAG 引用依據** | 比對到的詐騙樣態（kb-xxx），附**官方來源連結**可點 |
| **建議行動／查證問題** | 該怎麼做、可以反問賣家什麼 |
| **安全回覆腳本** | **可一鍵複製、直接貼回去反測對方**的安全話術 |
| **連結與電話查核** | 自動抽出網址／電話比對黑名單，附一鍵查證連結 |
| **查證／檢舉管道** | 撥 165、165 全民防騙網、蝦皮／FB 檢舉等一鍵連結 |

### 📍 其他位置
- **頁面上方**：「詐騙樣態圖鑑」按鈕 → 彈窗瀏覽全部 16 類詐騙樣態（學習用）。
- **頁面最下方「檢索推理軌跡」**：顯示 pseudo-query 與 HyDE，呈現判斷有檢索依據、不是只靠 LLM 直覺。

---

## 四、常見問題

| 狀況 | 原因 / 怎麼辦 |
|------|--------------|
| 右欄顯示「本機 4B LLM **未啟用**」 | 剛啟動時小模型在背景預熱（約 10–30 秒）。**稍等幾秒再按一次「分析風險」**即可；或在模式視窗按「重新檢查」 |
| 想用小模型卻沒有 | 在模式視窗「離線版」按「下載 LLM」下載一次（約 3.3GB），之後就會「已就緒」 |
| 沒有網路 | 用**離線版**：規則＋知識庫＋本機小模型全程離線可跑 |
| 連接埠被占用 | 換埠：`$env:PORT=9000; python server.py` |

---

## 五、一句話總結

> **左邊貼文字或截圖 → 按「分析風險」→ 右邊看風險等級、攻擊鏈與可直接貼回去的安全回覆。**
> 沒網路用離線版、要更強用線上版；每個判斷都看得到依據、可查證。
