# 專案運作說明（How It Works）

二手交易 AI 防踩雷助理：使用者貼上 FB / Threads / 蝦皮 的交易貼文、私訊或截圖文字，
系統判斷是否為詐騙，輸出風險等級、命中話術、RAG 引用依據、建議行動與官方查證連結。

本文件說明三件事：**資料從哪來**、**系統怎麼運作**、**一筆文字如何變成風險報告**。

---

## 1. 一句話總覽

採**混合式（hybrid）**研判，兩層互補、永不崩潰：

```
使用者文字（或截圖 OCR 後的文字）
   │
   ▼
①  規則引擎（離線、毫秒級、可解釋）  ──►  風險等級＋RAG引用＋話術標註
   │                                         ▲
   ▼                                         │ 取較高風險（保守融合）
②  4B LLM 語意研判（gemma3:4b，可選）───────┘
```

- **規則引擎永遠執行**：完全離線、毫秒級、附 165／警政署等真實來源引用。
- **4B LLM 在偵測到本機 Ollama 時自動加入**：抓規則漏掉的新話術；融合時**取較高風險**。
- **自動降級**：LLM 連不到或失敗（重試一次後）就退回純規則模式，demo 不會壞。

---

## 2. 資料來源（Data Sources）

系統有三類資料，全部是**可查證的真實來源**或**明確標示的示範規則**。

### 2.1 防詐知識庫 — `content/anti_fraud_knowledge_base.json`（RAG 話術資料庫）

> **這就是「話術資料庫」。格式為 JSON，目前 10 類樣態。**

每一筆對應一種詐騙樣態（或正常交易反例），來源如下：

| id | 樣態 | 資料來源 | 來源連結 |
|----|------|----------|----------|
| kb-001 | 假買家→假客服 | 165 全民防騙宣導整理（退輔會轉載） | vac.gov.tw |
| kb-002 | 被誘導金流驗證 | 移民署新住民全球新聞網（整理警政署／165 打詐儀表板） | immigration.gov.tw |
| kb-003 | 冒用物流／釣魚連結 | 內政部警政署新聞發布 | npa.gov.tw |
| kb-004 | 私下交易／站外匯款 | 內政部警政署新聞發布 | npa.gov.tw |
| kb-005 | 異常低價與急迫話術 | 專案示範規則（整理常見手法） | local |
| kb-006 | 安全交易守則（正常反例） | 165 反詐騙與平台交易安全宣導 | 165.npa.gov.tw |
| kb-007 | 解除分期付款詐騙（台灣最常見） | 165 全民防騙：解除分期付款 | 165.npa.gov.tw |
| kb-008 | 貨到付款換包裹／空包裹 | 內政部警政署 165 宣導 | 165.npa.gov.tw |
| kb-009 | 假投資保證獲利群組 | 165 反詐騙：假投資 | 165.npa.gov.tw |
| kb-010 | 代購代儲遊戲點數 | 專案示範規則（整理常見交易糾紛） | local |

> 誠實標示：kb-005、kb-010 沒有單一官方網址，是**整理常見手法的示範規則**（`source` 已註明）；
> 其餘 8 筆都附**真實政府／165 來源連結**，會顯示在報告「RAG 引用依據」裡可點擊查證。

每筆 JSON 的欄位：

| 欄位 | 意義 |
|------|------|
| `id` | 編號（報告引用時顯示，如 `kb-007`） |
| `title` | 樣態標題 |
| `source` / `source_url` | 資料來源說明與可點擊連結 |
| `risk_signals` | **核心話術關鍵詞陣列**（命中就加重風險，×4 加權） |
| `guidance` | 防詐建議（也參與語意比對） |

**新增一種詐騙樣態**：只要在 JSON 加一筆物件即可，不必改程式（改完需重啟伺服器，因知識庫啟動時快取）。

### 2.2 同義詞與詞表（讓比對抓得到變體）

| 資料 | 位置 | 內容 |
|------|------|------|
| 同義詞表 `SYNONYMS` | `src/text_normalize.py` | `轉帳→匯款`、`加賴→LINE`、`驗證碼→OTP`、`解除設定→解除分期`… |
| 高/中風險詞 `HIGH_TERMS` / `MEDIUM_TERMS` | `src/rules_engine.py` | `金流驗證/ATM/解除分期/保證獲利`…、`先付/訂金/低價`… |
| pseudo-query 觸發詞 `PSEUDO_PATTERNS` | `src/rules_engine.py` | 觸發「假客服金流驗證」「解除分期付款詐騙」等假想查詢標籤 |

### 2.3 測試案例 — `content/test_cases.json`

低/中/高、拆字規避、同義詞、正常交易等案例，用來驗證分級正確（`--test` 與 pytest 都會跑）。
此外，知識庫的話術關鍵詞與部分測試是**對照真實通報案例**校準的，來源包含：

- **165 全民防騙網**、**內政部警政署**、**蝦皮官方賣家中心**（假買家／假客服／釣魚宣導）
- **PTT**（WomenTalk、BabyMother、e-shopping 受害心得，含逐字話術片段）
- **Dcard**（網路購物版／反詐騙版的賣貨便、店到店詐騙討論）

> 例：「直接匯到我帳戶、不用透過蝦皮」「分期設定錯誤要你到 ATM 解除分期」等話術，
> 就是依這些真實通報的用字補進 `kb-004`、`kb-007` 的 `risk_signals`。

---

## 3. 一筆文字如何變成風險報告（運作邏輯）

以輸入「客服要我到網．路．銀．行做金．流．驗．證解除帳戶凍結」為例：

### 步驟 ①：文字正規化 `text_normalize.py`
- **NFKC 正規化**：全形轉半形（`ＡＴＭ`→`atm`、`ＮＴ＄`→`nt$`）。
- **compact 去分隔**：移除字元間標點，把「金．流．驗．證」還原成「金流驗證」→ **破解拆字規避**。
- **同義詞**：比對時把「加賴」視為「LINE」、「轉帳」視為「匯款」。
- **中文 bigram 斷詞**：「金流驗證」切成 `金流/流驗/驗證`，讓 RAG 的詞重疊在中文上真正有效。

### 步驟 ②：pseudo-query（假想查詢擴展）`rules_engine.pseudo_queries`
依關鍵詞把原文擴展成多個假想查詢標籤（如「假客服金流驗證」「解除分期付款詐騙」），增加檢索召回。

### 步驟 ③：HyDE（假想文件）`rules_engine.hyde_document`
產生一段「假想的詐騙描述」當推理軌跡展示。
> 注意：HyDE **只用於展示，不參與 RAG 計分**（它是通用模板，計分會污染所有條目）。

### 步驟 ④：RAG 檢索 `rules_engine.retrieve_evidence`
- 用「原文＋pseudo-query 標籤」對每筆知識庫算 **token 重疊** + **risk_signal 命中（×4 加權）**。
- 設相關性門檻過濾雜訊；取分數前 3 筆當「引用依據」（附 `source_url`）。
- 低風險（無任何詐騙訊號）時只引用「安全交易守則」(kb-006)，避免誤導。

### 步驟 ⑤：分級 `rules_engine.classify_risk`
```
高（75–95）：命中高風險詞（金流驗證/ATM/解除分期…）或知識庫命中訊號 ≥ 3
中（45–74）：命中中風險詞或知識庫命中訊號 ≥ 1
低（20）   ：無明顯話術
```
**安全語境抑制**：偵測到「不接受先匯款／可面交／現場測試」時，排除相關誤判。

### 步驟 ⑥：4B LLM 融合 `scam_guard_demo.analyze` + `llm_client.py`
- 若本機 Ollama 可用，把原文＋RAG 依據丟給 **gemma3:4b**，要求回傳 JSON（風險等級／信心／理由／是否規避）。
- **融合取較高風險**（保守）：規則與 LLM 取較高者；LLM 失敗自動重試一次，再失敗就用規則結果。
- 實測：像「直接匯到私人帳號繞過平台」規則判低、LLM 判高 → 綜合取高（雙向互補）。

### 步驟 ⑦：話術標註 `text_normalize.highlight_spans`
把命中的高/中風險詞在**原文中的位置**標出（容忍拆字規避），前端用顏色高亮。

### 最終輸出（JSON）
```
輸入文字、風險等級、風險分數、綜合說明、規則研判、LLM研判、
可疑訊號、引用到的防詐依據、命中話術標註、
pseudo_queries、HyDE假想文件、建議行動、可問賣家的查證問題
```

---

## 4. 前後端互動（含漸進式渲染）

LLM 在 CPU/GPU 推論需數秒，為了「即時更新」採**兩段式**：

```
瀏覽器                              server.py                         模組
  │  POST /api/analyze?llm=0          │                                │
  ├──────（只跑規則，~30ms）─────────►│ demo.analyze(use_llm=False)    │
  │◄───── 規則結果（中·60）──────────┤                                │
  │  立即顯示報告＋話術標註            │                                │
  │  徽章：「4B LLM 研判中…」          │                                │
  │  POST /api/analyze（含 LLM）       │                                │
  ├──────（~4–5 秒）─────────────────►│ demo.analyze(use_llm=True)     │──► gemma3:4b
  │◄───── 最終結果（高·80）──────────┤                                │
  │  分數與 LLM 欄自動更新             │                                │
```

- **漸進式渲染**：先用規則引擎瞬間回應，LLM 研判完再覆蓋分數。
- **請求序號防錯**：快速連點時舊回應不會蓋掉新結果。
- `/api/health` 回報 `llm_available`，前端據此決定離線版是否走兩段式。
- 伺服器送 `Cache-Control: no-store`，改前端後普通重整即可生效。

### 離線版 / 線上版（載入時 modal 選擇）

前端載入時會跳 modal 讓使用者選模式，請求 body 帶 `mode` 與 `geminiKey`：

| 模式 | LLM | 看截圖 | 金鑰 |
|------|-----|--------|------|
| **離線版** | 本機 Ollama `gemma3:4b` | tesseract.js（瀏覽器離線 OCR） | 不需 |
| **線上版** | 雲端 Gemini `gemini-2.5-flash` | **Gemini VLM**（截圖直送多模態） | 每次輸入、**不儲存** |

- 線上版由 `server.py` 代呼叫 Gemini（`POST /api/analyze`、看圖走 `POST /api/vlm`），
  避免前端直連的 CORS 與金鑰外露；key 僅存前端記憶體與當次請求，不寫 localStorage、
  不寫伺服器、不寫日誌。
- Gemini 採 **OpenAI 相容端點**，但路徑是 `.../v1beta/openai/chat/completions`
  （比 OpenAI 少一段 `/v1`），故 `llm_client.py` 有 Gemini 專用呼叫。
- 線上版看不到 key 或研判失敗時，**優雅退回規則模式**並於綜合說明標示。

---

## 5. 程式模組地圖

| 檔案 | 職責 |
|------|------|
| `src/text_normalize.py` | 正規化、同義詞、拆字規避、中文 bigram 斷詞、話術標註定位 |
| `src/rules_engine.py` | 離線規則引擎：pseudo-query → HyDE → RAG → 分級（**資料單一來源**） |
| `src/llm_client.py` | LLM 用戶端（Ollama／OpenAI 相容／Gemini，含 VLM 轉錄，純標準庫，失敗即降級、重試一次） |
| `src/scam_guard_demo.py` | 主入口（orchestrator）：規則＋LLM 融合、離線/線上模式選擇、VLM 入口、背景預熱、CLI、`--test` |
| `server.py` | 本機 HTTP 伺服器：`/api/analyze`（支援 `?llm=0`、`mode`、`geminiKey`）、`/api/vlm`、`/api/health` |
| `app/` | 前端：離線/線上模式切換、截圖 OCR/VLM、原文標註、規則 vs LLM 對照、查證連結、複製/列印 |
| `tests/test_scam_guard.py` | pytest 測試 |
| `n8n/` | 可匯入 n8n：判斷層 workflow + 黑名單每日同步排程（與 Python 引擎邏輯同步） |

---

## 6. 前端功能

- **截圖 OCR**：tesseract.js 前端離線辨識繁中（`runOcr`），並自動清理介面雜訊
  （`cleanOcrText`：去除時間列、FB「按讚留言分享」、浮水印；但保留 ATM/LINE/OTP 等關鍵字）。
- **原文話術標註**：把觸發風險的字句在原文標色，連拆字規避都能標位置。
- **規則 vs LLM 對照面板**：並排顯示兩種研判，呈現混合式價值。
- **下一步建議**：依風險等級給對應行動。
- **連結與電話查核（黑名單比對）**：`src/link_check.py` 自動抽出文字裡的網址／電話，採**分層判斷**：
  1. **先查本地黑名單**（`content/blocklist.json`，由排程每天更新）→ 命中即標「已被通報（官方通報）」高風險。
  2. 沒命中 → **啟發式判斷**（冒用品牌網域、原始 IP、短網址、可疑 TLD、境外電話）。
  3. 仍無線索 → 交給話術引擎 + LLM 判斷（「沒命中黑名單 ≠ 安全」）。
  每個項目附 Google 安全瀏覽／VirusTotal／Whoscall／165 一鍵查證連結；偵測到惡意連結會**保守提升**整體風險。
  - 黑名單由 `tools/sync_blocklist.py` 從**官方來源**（165 打詐儀表板、data.gov.tw）更新，可由 n8n 每天 08:00 排程觸發；只爬官方／政府開放資料，不爬 Whoscall 等商業服務。
  - 設環境變數 `SCAM_SAFEBROWSING_KEY` 可額外實際呼叫 Google Safe Browsing API。
- **查證管道**：報告內附官方連結 —— ☎ 撥打 165、165 全民防騙網、165 打詐儀表板
  （可查可疑網址／電話／帳號）。
- **輸出**：複製摘要、複製查證問題、下載 JSON、下載文字報告、列印／匯出 PDF。

---

## 7. 4B LLM（gemma3:4b）

- 以**免安裝版 Ollama**（位於 `C:\Users\USER\Downloads\ollama`）執行 `gemma3:4b`（Q4_K_M，約 3.3GB），Vulkan GPU 推論。
- 伺服器啟動會**背景預熱模型**，第一個請求不必承受冷啟動（預熱後每次分析約 4–5 秒）。
- LLM 僅用 Python 標準庫呼叫，無需額外套件；離線版模型與端點以環境變數設定（`SCAM_LLM_*`）。
- **線上版**改用雲端 Gemini `gemini-2.5-flash`（OpenAI 相容端點），文字研判與看截圖 VLM
  共用同一把使用者每次輸入的金鑰；同樣只用標準庫呼叫，失敗即退回規則模式。
  （註：`gemini-2.0-flash` 免費層額度已為 0，故預設改用仍有免費額度的 2.5 Flash。）

---

## 8. 怎麼跑

```powershell
# 一鍵：自動啟動 Ollama → 進入 4B LLM 模式 → 開瀏覽器
.\Start-UI.ps1

# 停止
.\Stop-UI.ps1

# 測試
python src\scam_guard_demo.py --test     # 分級檢查（8 案例）
python -m pytest tests\ -v               # 完整測試（37 項）
```

不啟動 Ollama 也能用，系統會自動以**離線規則模式**運作。
